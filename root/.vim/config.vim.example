" vim:ft=vim
" copy this file to config.vim

let g:ai_suggestion = 'copilot.vim'
" aiproxy (default) | kimichat | 01ai
let g:ai_service = '01ai'
let g:clangd_path = '/usr/bin/clangd'
if has('nvim')
else
	set grepprg=rg\ --vimgrep\ -uu
	set grepformat=%f:%l:%c:%m
endif
